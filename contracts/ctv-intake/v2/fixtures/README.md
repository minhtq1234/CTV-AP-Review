# CTV intake v2 synthetic fixtures

`schema-example` demonstrates closed document shapes only; it is not a materialized package. Semantic complete packages come from the production-backed Task 5 fixture factory.

`invalid-assignment` contains exactly one intentional cross-reference failure: `assignment-decision-missing`.
